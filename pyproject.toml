[project]
name = "ai-training-genworx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vmkmanoj", email = "vmkmanoj13@gamil.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "ruff>=0.16.2",
    "uvicorn>=0.52.1",
]

[project.scripts]
ai-training-genworx = "ai_training_genworx:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
